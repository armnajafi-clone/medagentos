# Error Handling

Three categories:

API errors:
invalid requests.

Workflow errors:
execution failures.

System errors:
database/storage failures.


Every error should include:

error code
message
trace id
